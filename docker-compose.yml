services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rag-engine-backend
    ports:
      - "8000:8000"
    env_file:
      - ./app/.env
    volumes:
      - ./app:/app/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
